# Demo footage

Drop your screen recordings here, named exactly:

- `hint.mov` — Hint Mode demo
- `scroll.mov` — Scroll Mode demo
- `search.mov` — Search Mode demo

Recording instructions: see [`../../SHOTLIST.md`](../../SHOTLIST.md).
Capture helper: `../../record-demo.sh`.

`.mov` files are git-ignored (they can be large) — they live locally only.
